import * as campaign from './project';
import * as card from './card';
import * as tab from './tab';

import { INTRO_PROJECT, INTRO_CARDS, INTRO_TABS } from './project/constants';

export const loadIntro = () => (dispatch) => {
};

export const copyCard = () => (dispatch) => {

};

export const deleteCard = () => (dispatch) => {};

export const linkCardToTab = () => (dispatch) => {};

export const unlinkCardFromTab = () => (dispatch) => {};

export const addTab = () => (dispatch) => {
  
};

export const deleteTab = () => (dispatch) => {};
